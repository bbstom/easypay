# 域名配置 - 快速修复指南

## 🎯 问题

构建时域名是硬编码的 `https://dd.vpno.eu.org`，需要改为 `https://kk.vpno.eu.org`。

## ✅ 解决方案（已完成）

已经修改了以下文件，现在支持从环境变量读取域名：

1. ✅ `scripts/update-domain.js` - 新增域名更新脚本
2. ✅ `scripts/generate-sitemap.js` - 修改为读取环境变量
3. ✅ `package.json` - 添加自动化构建流程

---

## 🚀 立即修复（3 步）

### 步骤 1：配置域名

编辑 `.env` 文件，添加或修改：

```bash
# 在 .env 文件中添加
SITE_URL=https://kk.vpno.eu.org
```

### 步骤 2：重新构建

```bash
npm run build
```

构建过程会自动：
- 更新 `index.html` 中的所有域名引用
- 生成 `sitemap.xml` 使用新域名

### 步骤 3：验证结果

```bash
# 检查 sitemap
npm run verify-sitemap

# 查看 index.html 中的域名
grep "kk.vpno.eu.org" index.html
```

---

## 📋 验证清单

构建完成后，检查以下内容：

### 1. index.html
```bash
grep "og:url" index.html
# 应该显示：https://kk.vpno.eu.org/
```

### 2. sitemap.xml
```bash
head -20 public/sitemap.xml
# 所有 <loc> 标签应该是 https://kk.vpno.eu.org
```

### 3. 构建输出
```
✅ 数据库连接成功
📝 找到 0 篇已发布的博客文章
✅ Sitemap 生成成功！
🔗 域名: https://kk.vpno.eu.org  ← 应该是新域名
```

---

## 🔧 如果还是旧域名

### 方法 1：清理缓存重新构建

```bash
# 清理
rm -rf dist node_modules/.vite

# 重新构建
npm run build
```

### 方法 2：手动更新

```bash
# 1. 更新 index.html
node scripts/update-domain.js

# 2. 生成 sitemap
npm run generate-sitemap

# 3. 构建
npm run build
```

### 方法 3：直接指定域名

```bash
SITE_URL=https://kk.vpno.eu.org npm run build
```

---

## 📝 .env 配置示例

完整的 `.env` 配置：

```bash
# 数据库
MONGODB_URI=mongodb://root:Ubuntu123!@172.16.254.100:27017/fastpay?authSource=admin

# 域名配置（新增）
SITE_URL=https://kk.vpno.eu.org
FRONTEND_URL=https://kk.vpno.eu.org
APP_URL=https://kk.vpno.eu.org

# 其他配置...
PORT=5000
JWT_SECRET=fastpay_secret_key_2024_secure_token
```

---

## 🎉 完成

配置完成后，以后每次构建都会自动使用正确的域名！

**下次更换域名**：
1. 修改 `.env` 中的 `SITE_URL`
2. 运行 `npm run build`
3. 完成！

---

## 📞 相关命令

```bash
# 查看当前配置的域名
cat .env | grep SITE_URL

# 手动更新域名
node scripts/update-domain.js

# 生成 sitemap
npm run generate-sitemap

# 验证 sitemap
npm run verify-sitemap

# 完整构建
npm run build
```
